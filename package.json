{
  "name": "car-detailer",
  "version": "1.0.0",
  "private": true,
  "description": "Car detailer job tracker — Neon + Netlify",
  "scripts": {
    "dev": "netlify dev",
    "hash-password": "node -e \"const b=require('bcryptjs');const p=process.argv[1]||'admin';b.hash(p,10).then(h=>console.log(h));\""
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.1"
  }
}
